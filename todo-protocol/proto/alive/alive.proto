syntax = "proto3";

package alive;

option go_package = "todo-back/infrastructure/handler/grpc/proto_go_gen/alive";

import "common/common.proto";

service Alive {
  rpc Ping(common.Empty) returns (common.Empty) {}
}
